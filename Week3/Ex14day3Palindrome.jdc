println("This programme can create a Palindrome");
println("Please enter a word or sentance below which we will convert for you:");
String entered = input.nextLine();
String converted = reverse(entered);
println(converted);

String reverse(String ent)
{

	int length = ent.length();
	String reverse = "";
	for(int i = length -1; i>=0; i--)
	{
		reverse += ent.charAt(i);
	}
	return reverse;
}
