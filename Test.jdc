String stri = input.nextLine();
int length = stri.length();
println(length);
println(stri);
println(stri.substring(0,1));



for (int count = 0; count<length; count++)
	{
String current = stri.substring(count,(count+1));
		if (current == "1"){
			println ("1");
		}
	}